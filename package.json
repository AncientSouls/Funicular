{
  "name": "ancient-funicular",
  "version": "4.0.0-beta",
  "description": "Abstract hierarchical life cycle of the mount.",
  "bugs": "https://github.com/AncientSouls/Funicular/issues",
  "repository": "AncientSouls/Funicular",
  "license": "MIT",
  "scripts": {
    "tsc": "rm -rf ./lib ./tests && tsc ./src/*/*.ts --outDir ./ --sourceMap -d true --module CommonJS",
    "test": "mocha ./tests/index.js --require source-map-support/register",
    "dev-link": "npm link ancient-mixins && npm link ancient-cursor",
    "dev-test": "(rm -rf .dev && git clone -b dev https://github.com/AncientSouls/AncientSouls.github.io.git .dev && cd .dev && git submodule init && git submodule update && npm run dev-install) && npm link ./ && (cd .dev && npm run dev-use-links && npm run dev-check)"
  },
  "dependencies": {
    "lodash": "^4.17.5",
    "ancient-mixins": "AncientSouls/Mixins#develop",
    "ancient-cursor": "AncientSouls/Cursor#develop"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.48",
    "@types/node": "^9.4.6",
    "async": "^2.6.0",
    "chai": "^4.1.2",
    "mocha": "^5.0.1",
    "source-map-support": "^0.5.3",
    "typescript": "^2.7.2"
  }
}
